Profile description
-------------------
This directory is used when a particular configuration is required when running the testware:
setting this target ---> -Dtaf.profiles = localInfo <--- properties are obtained from here.

The properties already defined in the taf_properties directory are overridden if you redefine it here.